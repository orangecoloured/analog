[build]
  functions = "netlify/functions"
  base = "/"
  command = "vite build"
  publish = "dist"

[functions."cleanUp"]
  schedule = "@daily"

[[redirects]]
  from = "/api"
  to = "/.netlify/functions/api/events"
  status = 200
